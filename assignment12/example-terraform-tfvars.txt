# ─────────────────────────────────────────────
#  terraform.tfvars  —  fill in your values
# ─────────────────────────────────────────────
# Copy this file to terraform.tfvars and edit before running.
# Do NOT commit terraform.tfvars to version control.

project_name = "financial-server"
environment  = "prod"
aws_region   = "us-east-1"

# Your VPC and subnet IDs (from AWS console or existing Terraform)
vpc_id    = "vpc-06c87c296368bd35c"
subnet_id = "subnet-01454a1d250631a5d"

# EC2 key pair name (must already exist in your AWS account)
key_name = "vockey"

# Instance type — t3.medium is a good baseline for financial apps
instance_type = "t3.medium"

# Ubuntu 22.04 LTS AMI — us-east-1
# Find the latest at: https://cloud-images.ubuntu.com/locator/ec2/
ami_id = "ami-0ec10929233384c7f"
